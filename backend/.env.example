# Copy this file to `.env` in the `backend` folder and fill in the values.
# Example values shown; change them to match your database credentials.

# MongoDB connection string. For local development you can use:
# MONGODB_URI=mongodb://127.0.0.1:27017/g4_sdn
MONGODB_URI=mongodb://127.0.0.1:27017/g4_sdn
PORT=9999
# Server port (optional)
# PORT=9999
